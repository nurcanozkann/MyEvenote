
@helper LinkButton(string url = "", string id = "", string color = "", string addtionalClass = "", string title = "", string icon = "", string text = "")
{
    <a href="@url" id="@id" name="@id" class="btn btn-@color @addtionalClass" title="@title">
        <span class="glyphicon glyphicon-@icon"></span> @text
    </a>
}

@helper LinkButton(string url = "", string color = "", string addtionalClass = "", string title = "", string icon = "", string text = "")
{
    <a href="@url" class="btn btn-@color @addtionalClass" title="@title">
        <span class="glyphicon glyphicon-@icon"></span> @text
    </a>
}

@helper Button(string id = "", string type = "button", string color = "", string addtionalClass = "", string icon = "", string text = "")
{
    <button type="@type" id="@id" name="@id" class="btn btn-@color @addtionalClass">
        <span class="glyphicon glyphicon-@icon"></span> @text
    </button>
}

@helper Button(string type = "button", string color = "", string addtionalClass = "", string icon = "", string text = "")
{
    <button type="@type" class="btn btn-@color @addtionalClass">
        <span class="glyphicon glyphicon-@icon"></span> @text v
    </button>
}

@helper LinkMenu(string url = "", string icon = "", string text = "")
{
    <a href="@url">
        <span class="glyphicon glyphicon-@icon"></span> @text
    </a>
}

@helper ModalPopup(string id = "modalPopup", string title = "Modal Title", bool showCloseButton = true)
{
    <div class="modal fade" id="@id" name="@id" tabindex="-1" role="dialog" aria-labelledby="@(id + "_label")" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header" id="@(id + "_header")">
                    <h5 class="modal-title" id="@(id + "_label")">@title</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" id="@(id + "_body")">
                    ...
                </div>
                <div class="modal-footer" id="@(id + "_footer")">
                    @if (showCloseButton)
                    {
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Kapat</button>
                    }
                </div>
            </div>
        </div>
    </div>
}